<%= form_with(model: wspedido) do |form| %>
  <% if wspedido.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wspedido.errors.count, "error") %> prohibited this wspedido from being saved:</h2>

      <ul>
        <% wspedido.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
